import { PubSubMessage } from './Messages/PubSubMessage';
import { SingleUserPubSubClient } from './SingleUserPubSubClient';
/**
 * A listener attached to a single PubSub topic.
 */
export declare class PubSubListener<T extends PubSubMessage = PubSubMessage> {
    private readonly _type;
    private readonly _userId;
    private readonly _callback;
    private readonly _client;
    /** @private */
    constructor(_type: string, _userId: string, _callback: (message: T) => void, _client: SingleUserPubSubClient);
    /**
     * The type of the topic.
     */
    get type(): string;
    /**
     * The user ID part of the topic.
     */
    get userId(): string;
    /**
     * Removes the topic from the PubSub client.
     */
    remove(): Promise<void>;
    /** @private */
    call(message: T): void;
}
