import { ApiClient } from '../../../ApiClient';
/** @private */
export interface HelixModeratorData {
    user_id: string;
    user_name: string;
}
/**
 * Information about the moderator status of a user.
 */
export declare class HelixModerator {
    private readonly _data;
    /** @private */
    protected readonly _client: ApiClient;
    /** @private */
    constructor(_data: HelixModeratorData, client: ApiClient);
    /**
     * The ID of the user.
     */
    get userId(): string;
    /**
     * Retrieves more data about the user.
     */
    getUser(): Promise<import("../User/HelixUser").HelixUser | null>;
    /**
     * The name of the user.
     */
    get userName(): string;
}
